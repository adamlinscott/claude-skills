{
  "name": "claude-skills",
  "version": "0.1.0",
  "private": true,
  "description": "Skills for Claude Code, linked into ~/.claude/skills by install.mjs.",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "//": "engines floor is not cosmetic: install.mjs points fs.rmSync(recursive, force) at directory junctions and symlinks, and a Node old enough to descend a reparse point instead of removing it would delete this repo's own skills/ contents.",
  "engines": {
    "node": ">=20"
  }
}
